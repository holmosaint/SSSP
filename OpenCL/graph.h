struct GraphData{
    // This contains a pointer to the edge list for each vertex
    int *vertexArray;
    
    // vertex count
    int vertexCount;
    
    // This contains pointers to the vertives that each edge is attached to
    int *edgeArray;
    
    // Edge count
    int edgeCount;
    
     // weight array
     int *weightArray;
                                 
};
